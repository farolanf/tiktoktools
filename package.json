{
  "name": "tiktoktools",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel watch manifest.json --config @parcel/config-webextension --host localhost",
    "build": "parcel build manifest.json --config @parcel/config-webextension",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Farolan Faisal <farolanfaisal@gmail.com>",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@parcel/config-webextension": "^2.8.0",
    "parcel": "^2.8.0"
  }
}
